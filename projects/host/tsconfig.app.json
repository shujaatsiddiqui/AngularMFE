{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "paths": {
      "auth-lib": ["../../dist/auth-lib"]  // Adjust this path relative to each app's `tsconfig.app.json`
    },
    "outDir": "../../out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts",
  ]
}
